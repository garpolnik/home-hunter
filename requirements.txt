# Core dependencies - pinned to specific versions for reproducibility and security.
# Only update versions after verifying no known vulnerabilities (pip-audit).
requests==2.32.3
pandas==2.2.3
pydantic==2.10.6
pyyaml==6.0.2
jinja2==3.1.5
sendgrid==6.11.0
python-Levenshtein==0.26.1

# Web GUI
flask==3.1.0
